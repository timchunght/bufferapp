require 'spec_helper'

describe Bufferapp do
  it 'has a version number' do
    expect(Bufferapp::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
